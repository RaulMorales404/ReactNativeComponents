import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    title: {
        fontSize: 25,
        fontWeight: 'bold',
        color: 'black',
    },
    container: {
        flex: 1,
        paddingHorizontal: 30,
    },
});
